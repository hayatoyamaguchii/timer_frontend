import { default as Timer } from "../components/timer";

const Home = () => {
    return (
        <div>
            <h1>home!!!</h1>
            <Timer />
        </div>
    );
};

export default Home;
